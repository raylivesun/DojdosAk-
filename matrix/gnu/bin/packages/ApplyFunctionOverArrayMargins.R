#!/usr/bin/r

# in todo Brazil happy
x <- cbind(x1 = 12, x2 = c(4:2, 2:4))
# expert measure logical Brazil and all country
dimnames(x); 
# verify snack measure level logical type
dim(x)
# column sums of values in tokens
# Same as get("*"):
match.fun("*")
# Overwrite outer with a vector
outer <- 1:5
match.fun(outer) # finds it anyway
is.function(match.fun("outer")) # as well
